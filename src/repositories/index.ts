export const create = async (data: any): Promise<any> => {
    // Create method with prisma
};

export const update = async (id: string, data: any): Promise<any> => {
    // Update method with prisma
};

export const remove = async (id: string): Promise<any> => {
    // Remove method with prisma
};

export const activate = async (id: string): Promise<any> => {
    // Activate method with prisma
};

export const inactivate = async (id: string): Promise<any> => {
    // Inactivate method with prisma
};

export const getAll = async (filters: Record<string, unknown>): Promise<any> => {
    // GetAll method with prisma
};

export const getById = async (id: string): Promise<any> => {
    // GetById method with prisma
};
